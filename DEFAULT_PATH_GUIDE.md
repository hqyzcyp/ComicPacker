# 默认路径配置功能

## 新增功能

在文件浏览器面板顶部添加了**默认起始路径**设置功能，让您可以设定每次打开页面时自动跳转的目录。

## 使用方法

### 1. 设置默认路径

在文件浏览器面板顶部的"默认起始路径"输入框中：
- 输入您想要的默认路径，例如：`/vol2/1000/qb/down/comic/`
- 点击 💾 按钮保存
- 或按 Enter 键快速保存

### 2. 跳转到默认路径

点击 🏠 按钮可以快速跳转到已保存的默认路径。

### 3. 清除默认路径

清空输入框后点击 💾 按钮即可清除默认路径设置。

## 功能特点

✅ **自动加载** - 每次打开页面自动跳转到默认路径  
✅ **持久化存储** - 使用浏览器 localStorage 保存设置  
✅ **即时通知** - 操作成功后显示通知提示  
✅ **快捷键支持** - 输入框内按 Enter 键快速保存  

## 技术实现

- **前端存储**: 使用 `localStorage` API 持久化保存路径
- **通知系统**: 自定义通知组件，支持成功/信息/警告/错误四种类型
- **UI设计**: 深色主题，与整体界面风格一致

## 示例

```
默认路径: /vol2/1000/qb/down/comic/
```

设置后，每次打开 Web 界面都会自动导航到此目录。
